import Spinner from '@/components/Loading/Loading';
import React from 'react';

const loading = () => {
    return (
        <div>
            <Spinner/>
        </div>
    );
};

export default loading;